import 'package:flutter/material.dart';

class NewArgumentButton extends StatelessWidget {
  const NewArgumentButton({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return TextButton(
      onPressed: () {},
      child: const Text("+ Arg"),
    );
  }
}
